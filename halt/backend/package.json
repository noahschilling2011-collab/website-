{
  "name": "halt-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "HALT backend — real PSD2 account monitoring + Twilio escalation",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test test/*.test.mjs",
    "test-alert": "node scripts/testAlert.mjs",
    "demo-scan": "node scripts/demoScan.mjs"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.2"
  }
}
